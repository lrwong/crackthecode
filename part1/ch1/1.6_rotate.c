#include<stdio.h>
#include<stdlib.h>


int main(int argc, * argv[]){
	int n = 5; //size of the NxN matrix
	int mat[n][n];

	int x, y,

}

int rotate(int *mat){
	int i, j, x, y, layer;
	int length = sizeof(mat[0])/sizeof(int);
	int out[length][length];


	layer  = 0;
	for(x = 0; x < length - layer; x++){
		for(y = 0; y < length - layer; y++){
			out[length-layer-x]
		}
	}

}